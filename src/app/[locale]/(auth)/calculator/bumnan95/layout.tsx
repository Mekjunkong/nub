import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bumnan 95",
  description: "Plan pension income with annuity analysis to close your retirement gap through age 95",
};

export default function Bumnan95Layout({ children }: { children: React.ReactNode }) {
  return children;
}
